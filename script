local Library = loadstring(game:HttpGetAsync("https://github.com/ActualMasterOogway/Fluent-Renewed/releases/latest/download/Fluent.luau"))()
local SaveManager = loadstring(game:HttpGetAsync("https://raw.githubusercontent.com/ActualMasterOogway/Fluent-Renewed/master/Addons/SaveManager.luau"))()
local InterfaceManager = loadstring(game:HttpGetAsync("https://raw.githubusercontent.com/ActualMasterOogway/Fluent-Renewed/master/Addons/InterfaceManager.luau"))()

local Window = Library:CreateWindow{
    Title = `Fluent {Library.Version}`,
    SubTitle = "by Actual Master Oogway",
    TabWidth = 160,
    Size = UDim2.fromOffset(830, 525),
    Resize = true,
    MinSize = Vector2.new(470, 380),
    Acrylic = true,
    Theme = "Dark",
    MinimizeKey = Enum.KeyCode.RightControl
}

local Tabs = {
    Main = Window:CreateTab{
        Title = "Main",
        Icon = "phosphor-users-bold"
    },
    Settings = Window:CreateTab{
        Title = "Settings",
        Icon = "settings"
    },
    Visuals = Window:CreateTab{
        Title = "Visuals",
        Icon = "eye"
    }
}

local Options = Library.Options

-- Keybinds Data
local keybindsList = {
    { Name = "Fly", Key = "F" },
    { Name = "Noclip", Key = "N" },
    { Name = "Speed Boost", Key = "LeftShift" }
}

-- Функция для отображения Keybinds
local function updateKeybindsDisplay()
    local keybindsText = ""
    for _, keybind in pairs(keybindsList) do
        keybindsText = keybindsText .. string.format("[Keybind] %s - %s\n", keybind.Name, keybind.Key)
    end
    return keybindsText
end

-- Вкладка Visuals: Keybinds Display
local keybindsParagraph = Tabs.Visuals:CreateParagraph("KeybindsDisplay", {
    Title = "Keybinds",
    Content = updateKeybindsDisplay(),
    TitleAlignment = "Left",
    ContentAlignment = Enum.TextXAlignment.Left
})

-- Вкладка Visuals: Переключатель Keybinds
local KeybindsToggle = Tabs.Visuals:CreateToggle("KeybindsToggle", {
    Title = "Show Keybinds",
    Default = false,
    Callback = function(Value)
        if Value then
            keybindsParagraph:SetValue(updateKeybindsDisplay())
            Library:Notify{
                Title = "Keybinds",
                Content = "Keybinds are now visible.",
                Duration = 3
            }
        else
            keybindsParagraph:SetValue("Keybinds are hidden.")
            Library:Notify{
                Title = "Keybinds",
                Content = "Keybinds are now hidden.",
                Duration = 3
            }
        end
    end
})

-- Addons:
SaveManager:SetLibrary(Library)
InterfaceManager:SetLibrary(Library)
SaveManager:IgnoreThemeSettings()
SaveManager:SetIgnoreIndexes{}
InterfaceManager:SetFolder("FluentScriptHub")
SaveManager:SetFolder("FluentScriptHub/specific-game")
InterfaceManager:BuildInterfaceSection(Tabs.Settings)
SaveManager:BuildConfigSection(Tabs.Settings)

Window:SelectTab(1)

Library:Notify{
    Title = "Fluent",
    Content = "The script has been loaded.",
    Duration = 8
}

SaveManager:LoadAutoloadConfig()
