-- Создаем Watermark
local screenGui = Instance.new("ScreenGui")
screenGui.Parent = game.Players.LocalPlayer:WaitForChild("PlayerGui")

local watermarkFrame = Instance.new("Frame")
watermarkFrame.Size = UDim2.new(0, 200, 0, 50)
watermarkFrame.Position = UDim2.new(0.1, 0, 0.05, 0) -- Позиция по умолчанию
watermarkFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
watermarkFrame.BorderSizePixel = 0
watermarkFrame.Visible = true
watermarkFrame.Parent = screenGui

local watermarkText = Instance.new("TextLabel")
watermarkText.Text = "NL | About137 | Best Hvt Community Config | 40 ms"
watermarkText.Size = UDim2.new(1, 0, 1, 0)
watermarkText.Position = UDim2.new(0, 0, 0, 0)
watermarkText.BackgroundTransparency = 1
watermarkText.TextColor3 = Color3.fromRGB(255, 255, 255)
watermarkText.Font = Enum.Font.SourceSansBold
watermarkText.TextSize = 16
watermarkText.TextXAlignment = Enum.TextXAlignment.Left
watermarkText.TextYAlignment = Enum.TextYAlignment.Center
watermarkText.Parent = watermarkFrame

-- Функция для обновления текста Watermark
local function updateWatermark()
    local ping = math.random(30, 50) -- Пример пинга (можно заменить на реальный)
    watermarkText.Text = string.format("NL | About137 | Best Hvt Community Config | %d ms", ping)
end

-- Обновляем Watermark каждую секунду
while true do
    updateWatermark()
    wait(1)
end

-- Функция для перемещения Watermark
local dragging = false
local dragInput, dragStart, startPos

watermarkFrame.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 then
        dragging = true
        dragStart = input.Position
        startPos = watermarkFrame.Position

        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                dragging = false
            end
        end)
    end
end)

watermarkFrame.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement then
        dragInput = input
    end
end)

game:GetService("UserInputService").InputChanged:Connect(function(input)
    if dragging and input == dragInput then
        local delta = input.Position - dragStart
        watermarkFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)
